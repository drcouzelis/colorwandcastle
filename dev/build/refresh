#!/usr/bin/bash

source "$(dirname ${0})"/settings

"${bin_dir}"/wipe && \
mkdir -p "${working_dir}"/build && cd "${working_dir}"/build && \
autoreconf --install "${source_dir}" && \
"${source_dir}"/configure --enable-silent-rules --prefix="${working_dir}"
