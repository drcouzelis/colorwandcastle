#!/usr/bin/bash

[ "${EUID}" -eq 0 ] && echo "ERROR: Do not run as root." && exit 1

source "$(dirname ${0})"/settings

# Debug

# Build and run through the GNU debugger
"$(dirname ${0})"/build || exit 1
gdb -ex run --args "${BUILD_workarea_path}"/bin/${BUILD_project} "$@"
