#!/usr/bin/bash

[ "${EUID}" -eq 0 ] && echo "ERROR: Do not run as root." && exit 1

source "$(dirname ${0})"/settings

# Clean

if [ -d "${BUILD_workarea_path}"/build ]; then
    # Delete old build objects
    make -C "${BUILD_workarea_path}"/build clean
fi
